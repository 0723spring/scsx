# MVP 基础环境：后端接口、图片处理、虚拟数据生成、测试。
# 安装命令：
#   python -m pip install -r requirements.txt

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
python-multipart>=0.0.9,<1.0

pillow>=10.0,<13.0
numpy>=1.26,<3.0
faker>=25.0,<40.0

pytest>=8.0,<9.0
httpx>=0.27,<1.0
